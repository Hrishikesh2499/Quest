import { colors } from "../../../theme";

const tabStyles = {
  tab: { color: colors.COLOR_WHITE },
  appBar: {
    border: 1,
    p: "8px",
    display: "flex",
    justifyContent: "center",
  },
};

export default tabStyles;
